
#include "ft_get_next_line/get_next_line.h"
#include "libft/libft.h"
#include "so_long.h"

int main (int ac , char **av)
{
    int bert;
    if(ac > 2)
        return NULL;
    bert = compber(av[1]);
    if  ( bert == 1)
        return NULL;
   
}